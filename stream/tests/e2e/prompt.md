Write a Markdown document only.

Requirements:
- Include an H1 title.
- Include at least two H2 sections.
- Include one fenced TypeScript code block (10-20 lines).
- No extra commentary outside the markdown content.
